# Claude 开发指南

## 基本规则

1. **语言**：所有回答都使用中文
2. **代码提交**：不要擅自提交代码，可以询问我是否要提交代码
3. **代码运行**：你不需要运行代码，你改完代码通知我就行，我自己运行查看效果
4. **开发流程**：接收到需求后，你先理解并给出实现方案，询问我是否同意，同意后你再开发
5. **方案质量**：每次提供方案时，必须分析多种可能的解决方案，比较优缺点，并推荐最优解（考虑性能、可维护性、安全性、开发成本等因素）
6. **严格遵循开发流程**：绝对不允许在未经用户同意的情况下直接实现方案。必须先给出方案分析，等待用户确认后再动手开发。违反此规则将被视为严重错误

## 项目文档

- **设计文档**：`docs/plans/20260203-MVP.md`
- **UI 设计规范**：`docs/standards/UI设计规范.md`
- **代码编写规范（核心）**：`docs/standards/代码编写规范-核心.md` ⭐
- **代码编写规范（详细）**：`docs/standards/代码编写规范.md`
- **全局样式变量**：`APP/app.wxss`

## 🎯 必须遵循的核心规范

### 代码风格
- 页面层级不要嵌套过深（不超过 6 层），使用精简的方式实现
- **优先使用 vant-weapp 组件库**，能使用的就使用
- 必须使用 CSS 变量（定义在 `APP/app.wxss`），不要硬编码颜色和间距
- 优先使用 `async/await`，必须用 `try-catch` 处理错误
- 常用的、可复用的工具函数封装到 `APP/utils` 目录下，供全局使用

### UI 规范
- 页面背景必须使用 `--bg-color` (#FAFAF8)，不要使用纯白
- 卡片圆角使用 `--radius-lg` (24rpx)
- 去线化设计：通过间距和背景色块区分内容，避免使用横线边框
- 可点击区域必须添加 `hover-class` 效果

### 数据库操作
- 使用软删除（`isDeleted: true`），不要硬删除数据
- 查询时限制返回字段，使用分页加载
- 多个操作用事务保证数据一致性

### 云函数
- 统一返回格式：`{ success: true/false, data/errorCode/errorMessage }`
- 必须进行参数校验和权限校验

---

**开发前请先阅读**：`docs/plans/代码编写规范-核心.md`